<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>User List with Actions</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .active {
            background: blue;
            border-radius: 20px;
            color: white;
        }

        .user-card {
            background-color: white;
            border: 1px solid #dee2e6;
            border-radius: 10px;
            padding: 15px;
            margin-bottom: 15px;
            transition: transform 0.2s ease-in-out;

        }

        .user-card:hover {
            transform: scale(1.02);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .verified {
            font-size: 0.9rem;
            font-weight: bold;
            color: green;
        }

        .not-verified {
            font-size: 0.9rem;
            font-weight: bold;
            color: red;
        }

        .btn-actions {
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div>
            <ul class="nav" style="padding-left: 10px; padding-top: 10px;">
                <li class="nav-item">
                    <a class="nav-link" href="/new-chalan">New Chalan</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/chalan-list">Chalan List</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/parti-master">Parti Master</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/inventory-master">Inventory Master</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/authorize-user">Authorize User</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="/barcode">Generate QR</a>
                </li>
            </ul>
        </div>
        <div class="container my-5">
            <div class="container" style="max-width: 400px;">
                <form action="/barcode" method="post">
                    <label for="inputPassword5" class="form-label">Data: </label>
                    <input list="suggestion" type="text" name="data" id="inputPassword5" class="form-control"
                        aria-describedby="passwordHelpBlock">
                    <datalist id="suggestion">
                            <% parti.forEach((ele)=> { %>
                                <option value="<%= ele.name %>" data-id="<%= ele.id %>">
                                    <%= ele.name %>
                                </option>
                                <% }) %>
                    </datalist>
                    <label for="inputPassword5" class="form-label">Brand Name</label>
                    <input type="text" name="brand" value="ANANDKUMARBHARATKUMAR" id="inputPassword5" class="form-control"
                        aria-describedby="passwordHelpBlock">   
                    <input class="btn btn-primary mt-2" type="submit" value="submit">
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
</body>

</html>